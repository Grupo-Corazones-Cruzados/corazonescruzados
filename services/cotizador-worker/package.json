{
  "name": "gcc-cotizador-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Worker dedicado del Agente de Cotizaciones Software (Claude Agent SDK, Opus 4.8). Mantiene la sesion viva y la reanuda por sessionId. Aislado de la app web (deps propias: zod 4 + agent SDK).",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.217",
    "pg": "^8.13.0",
    "zod": "^4.0.0"
  }
}

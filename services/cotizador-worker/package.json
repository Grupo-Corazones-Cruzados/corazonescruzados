{
  "name": "gcc-cotizador-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Worker dedicado del Agente de Cotizaciones Software (Agents SDK de OpenAI sobre gpt-5.6-luna). Mantiene la conversacion viva y la reanuda por sessionId. Aislado de la app web (deps propias: zod 4 + agents SDK).",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node index.mjs",
    "dev": "node --env-file=../../.env --env-file=../../.env.local index.mjs"
  },
  "dependencies": {
    "@openai/agents": "^0.17.0",
    "pg": "^8.13.0",
    "zod": "^4.0.0"
  }
}
